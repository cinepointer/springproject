<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cinepointer.dao.userDao">

    <!-- 회원 단건 조회 -->
    <select id="selectUserById" resultType="com.cinepointer.dto.usersDto">
        SELECT 
            user_id,
            user_passwd,
            user_name,
            user_email,
            user_status,
            user_land_cd,
            user_birth_date
        FROM users
        WHERE user_id = #{user_id}
    </select>

    <!-- 회원 등록 -->
    <insert id="insertUser" parameterType="com.cinepointer.dto.usersDto">
        INSERT INTO users (
            user_id,
            user_passwd,
            user_name,
            user_email,
            user_status,
            user_land_cd,
            user_birth_date
        ) VALUES (
            #{user_id},
            #{user_passwd},
            #{user_name},
            #{user_email},
            #{user_status},
            #{user_land_cd},
            #{user_birth_date}
        )
    </insert>

    <!-- 회원정보 수정 -->
    <update id="updateUser" parameterType="com.cinepointer.dto.usersDto">
        UPDATE users
        SET
            user_passwd = #{user_passwd},
            user_name = #{user_name},
            user_email = #{user_email},
            user_status = #{user_status},
            user_land_cd = #{user_land_cd},
            user_birth_date = #{user_birth_date}
        WHERE user_id = #{user_id}
    </update>

    <!-- 회원 삭제 -->
    <delete id="deleteUser" parameterType="string">
        DELETE FROM users WHERE user_id = #{user_id}
    </delete>

    <!-- 전체 회원 리스트 조회 -->
    <select id="selectAllUsers" resultType="com.cinepointer.dto.usersDto">
        SELECT 
            user_id,
            user_passwd,
            user_name,
            user_email,
            user_status,
            user_land_cd,
            user_birth_date
        FROM users
        ORDER BY user_id
    </select>

</mapper>
